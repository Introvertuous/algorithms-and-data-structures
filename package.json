{
    "name": "algorithms_and_data_structures",
    "version": "0.0.1",
    "description": "Algorithms and data structures in various programming languages.",
    "repository": {
        "type": "git",
        "url": "https://github.com/Introvertuous/algorithms_and_data_structures"
    },
    "scripts": {
        "build": "rm -rf ./dist/ && rm -rf ./tsDist/ && tsc",
        "test": "jest src"
    },
    "devDependencies": {
        "jest-cli": "^15.1.1"
    },
    "dependencies": {
        "typescript": "^2.2.2"
    },
    "jest": {
        "testEnvironment": "node"
    }
}
